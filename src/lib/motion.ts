'use client';

import {
    motion as framerMotion,
    AnimatePresence,
    useScroll,
    useSpring,
    useTransform,
    useMotionValue,
    useInView,
    useAnimation
} from 'framer-motion';

// Re-export everything
export {
    framerMotion as motion,
    AnimatePresence,
    useScroll,
    useSpring,
    useTransform,
    useMotionValue,
    useInView,
    useAnimation
};
